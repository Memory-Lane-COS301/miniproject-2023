import { appNotificationPageDataAccess } from './app-notification-page-data-access';

describe('appNotificationPageDataAccess', () => {
  it('should work', () => {
    expect(appNotificationPageDataAccess()).toEqual('app-notification-page-data-access');
  });
});
