import { appNotificationPageFeature } from './app-notification-page-feature';

describe('appNotificationPageFeature', () => {
  it('should work', () => {
    expect(appNotificationPageFeature()).toEqual('app-notification-page-feature');
  });
});
